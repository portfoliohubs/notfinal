# خطة إصلاح أمنية وتقنية لمشروع PortfolioHubs

> ملف متابعة مستقل عن `PORTFOLIOHUBS-PLAN-ENDPOINT.md`. الغرض منه تتبع الإصلاحات المرتبطة بمراجعة المشروع، وتحديد المتبقي بدقة.
>
> تاريخ إنشاء خط الأساس: 2026-09-26
> الحالة العامة: **الإصلاح جارٍ**. المراحل غير المعلّمة أدناه لم تُنفذ بعد.

## طريقة المتابعة

1. ابدأ من أول بند غير مكتمل في المرحلة النشطة، ولا تعتبر بندًا منجزًا قبل تحقق اختبار قبوله.
2. بعد **كل تعديل برمجي منفرد**، حدّث حالة البند وسجّل الملف/الموضع والتغيير ونتيجة التحقق في «سجل التغييرات» أدناه.
3. إذا أظهر الاختبار خللًا، أعد الحالة إلى «جارٍ» واكتب الإجراء المتبقي؛ لا تضع علامة الإنجاز لمجرد أن التعديل اكتمل.
4. عند تعذر التحقق، استخدم «محجوب» واكتب السبب والأداة المطلوبة. لا تفترض نجاح النشر من نجاح البناء المحلي.
5. لا تضع أسرارًا أو رموز وصول أو بيانات مستخدمين في هذا الملف.

**مقياس الحالات:** `[ ] لم يبدأ`، `[~] جارٍ`، `[x] منجز ومتحقق`، `[!] محجوب`.

## خط الأساس

- أزيل منح صلاحية المسؤول بناءً على لاحقة البريد من قواعد Firebase والـWorker والواجهتين؛ يلزم اختبار التفويض قبل اعتبار الإصلاح متحققًا.
- أضيفت مصادقة إلى مسارات Express الإدارية والتحقق الأساسي من المدخلات؛ اختبارات API وتحديد المعدل ما زالا متبقيين.
- صُححت صياغة استعلام Firestore في `storage.rules` إلى `firestore.exists(...)`؛ لم تُختبر القواعد بالمحاكي بعد.
- أضيف فرض خادمي لحصة الحالات ومنح البرومو في الـWorker؛ يلزم تشغيل اختبارات D1 والتزامن.
- أُتيح Node.js 22.14 وnpm 10.9 وJava 21 مؤقتًا تحت TEMP، وثُبت Firebase CLI مؤقتًا عبر npm؛ `git` ما زال غير متاح في `PATH`.
- لم يعثر الفحص السابق على ملفات اختبارات `test/spec`؛ يجب إعادة البحث عند تهيئة بيئة التطوير.

## المرحلة 0: تجهيز بيئة تحقق آمنة

**الحالة: [~] أدوات التحقق متاحة مؤقتًا؛ Git واختبارات API ما زالت متبقية**

- [ ] تأكيد الفرع/نسخة العمل الحالية والاحتفاظ بنسخة من الملفات قبل الإصلاحات.
- [~] Node.js وnpm متاحان مؤقتًا، ونُفذ `npm ci --legacy-peer-deps`؛ Firebase CLI وJava استُخدما من TEMP للاختبارات.
- [x] `npm run lint` و`npm run build` نجحا بعد التعديلات؛ ظهرت تحذيرات Vite وchunk كبير موثقة في السجل.
- [x] تشغيل Firebase Emulator Suite واختبار Firestore وStorage Rules محليًا على مشروع `demo-portfoliohubs`، دون لمس الإنتاج.
- [ ] إضافة/تحديد طريقة اختبار API محلية وتوثيق متغيرات البيئة المطلوبة من دون إدراج قيم الأسرار في المستودع.

**معيار القبول:** يمكن تشغيل فحوصات المشروع والمحاكيات محليًا، والنتائج الأساسية محفوظة في سجل التغييرات. لا يبدأ نشر إصلاحات القواعد قبل توفر اختبار محلي لها.

## المرحلة 1: إغلاق تصعيد صلاحيات المسؤول

**الأولوية: حرجة — الحالة: [~] المصدر موحد؛ اختبارات واجهات Worker/Express وتدفق الدخول متبقية**

- [x] توحيد المسؤولين على قائمة الحسابات الثلاثة الصريحة نفسها في Login/AdminDashboard وExpress وWorker وFirestore وStorage؛ أزيلت لاحقة النطاق و`ADMIN_EMAILS` وسجلات المسؤول الديناميكية من قرار التفويض.
- [~] اشتراط البريد الموثق في جميع الطبقات؛ محاكي Firestore اختبر البريد غير الموثق، واختبارات واجهات Worker/Express متبقية.
- [x] إزالة إنشاء وثائق المسؤول تلقائيًا من `AdminDashboard`؛ إضافة مسؤولين جدد تحتاج آلية موحدة موثوقة لم تُنفذ بعد.
- [x] تقييد القراءة على `/admins/{email}` إلى المالك أو المسؤول؛ اختبار المحاكي أكد سماح قراءة المسار الذاتي ورفض قراءة وثيقة مسؤول آخر للمستخدم العادي.
- [~] توحيد Login وAdminDashboard مع قائمة الحسابات الموثقة؛ اختبار الدخول ورفض الحسابات غير المصرح بها عبر المتصفح/الـAPI متبقٍ.

**معيار القبول:** حساب عادي، وحساب غير موثق، وحساب يحمل لاحقة النطاق دون سماح صريح لا يمكنه الوصول إلى عمليات المسؤول؛ حساب مسؤول مصرح به فقط ينجح. اختبارات قواعد Firestore والـWorker تثبت الحالات الأربع.

## المرحلة 2: حماية واجهات Express العامة

**الأولوية: حرجة عند إتاحة الخادم للإنترنت — الحالة: [~] المصادقة ومعدل Express اختُبرا؛ تعميم المعدل والتحقق المتبقيان**

- [x] مصادقة وتفويض `/api/admin/generate-doctor-html`؛ اختبارات API أكدت 401/403 ووصول المسؤول المصرح إلى تحقق الحمولة.
- [x] حماية `/api/notify-indexnow`؛ الاختبار أكد رفض الروابط خارج الأصل وإرجاع 429 بعد الحد.
- [~] التحقق من بنية كائن الطبيب وقائمة لا تتجاوز 100 حالة؛ تحقق بقية الحقول ما زال مطلوبًا.
- [~] حدود JSON 10MB وurlencoded 1MB؛ محدد معدل 20 طلبًا/دقيقة لكل UID ومسار اختُبر، لكنه في الذاكرة لكل عملية فقط وليس مشتركًا بين النسخ.
- [~] إخفاء تفاصيل أخطاء التوليد/IndexNow وإضافة `nosniff` و`Referrer-Policy`؛ مراجعة الرؤوس الإضافية متبقية.
- [ ] التحقق من تزامن الكتابة إلى ملفات JSON وsitemap لتجنب تلف البيانات عند الطلبات المتزامنة.

**معيار القبول:** الطلب غير المصادق يفشل دون تغيير أي ملف؛ طلب المستخدم غير الإداري يفشل؛ المدخلات غير الصالحة/الكبيرة تُرفض؛ لا تُرسل روابط خارج النطاق المسموح.

## المرحلة 3: فرض الصلاحيات وحدود الباقات في الخادم

**الأولوية: عالية — الحالة: [~] حماية الحد والبرومو واختبار SQLite-backed Worker ناجحة؛ تحقق Cloudflare D1/staging متبقٍ**

- [~] منع تحديث المستخدم لحقول الامتيازات مثل `caseLimit` أو حالة الموافقة عبر تحديث الملف العام.
- [~] فرض حد الحالات عند إنشاء سجل جديد في الـWorker وقراءة الحد من ملف المستخدم الموثوق؛ اختبار محلي أكد 3 حالات ورفض الرابعة، ويلزم تكراره في D1 Worker.
- [~] تقييد `/api/publish` للمسؤول مؤقتًا؛ لا يوجد مستدعٍ له في الواجهة، ويلزم اختبار API وربطه لاحقًا بمصدر موافقة موثوق.
- [ ] الحفاظ على فحص ملكية الحالة وربط تحديث الحدث بعملية الحفظ ذريًا لتجنب تجاوز الحد بطلبات متزامنة.
- [~] إضافة جدول فريد `(code, uid)` وربط التسجيل والعداد والمنح في دفعة مشروطة؛ Worker الحقيقي اختُبر فوق SQLite ومخططات migration وأكد منع التكرار والحد، ويبقى تحقق Cloudflare D1 وتطبيق migration في staging.
- [ ] تحديد صلاحيات القراءة العامة لكل مجموعة، والتأكد من عدم نشر بيانات المستخدم الخاصة ضمن صفحات أو سجلات عامة.

**معيار القبول:** طلب API مباشر لا يرفع حد المستخدم ولا يتجاوز عدد الحالات؛ الرمز المنتهي/المستهلك لا ينجح؛ محاولات التزامن لا تتجاوز حدود الاستحقاق.

## المرحلة 4: إصلاح قواعد Firebase وتأمين رفع الوسائط

**الأولوية: عالية — الحالة: [~] Storage Rules اجتازت المحاكي؛ تحقق رفع Worker/ImageKit متبقٍ**

- [x] تعديل استعلام Firestore داخل Storage Rules إلى `firestore.exists(...)`؛ اختبارات المحاكي أكدت قراءة الوسائط لصفحة منشورة.
- [x] فرض أنواع JPEG/PNG/WebP وحد 5MB في Storage Rules؛ اختبارات المحاكي رفضت النوع غير المدعوم والملف الأكبر من الحد.
- [~] فرض حد 5MB وأنواع صور مسموحة في الـWorker، وتجاهل مجلد العميل وتنظيف الاسم؛ اختبار الرفع متبقٍ.
- [~] التحقق من مسار ImageKit وملكية الملف قبل تسجيله وربط الملف بحساب آخر؛ يلزم اختبار التكامل الفعلي.
- [~] وُحدت قائمة المسؤولين الثابتة والبريد الموثق بين Firestore وStorage والـWorker؛ اختبارات واجهات Worker وExpress متبقية.

**معيار القبول:** قواعد Storage وFirestore تنجح في المحاكي؛ يُسمح بالملفات المسموحة ضمن الحد فقط؛ تُرفض الملفات الكبيرة أو غير المدعومة ومحاولات القراءة/الكتابة عبر مستخدم آخر.

## المرحلة 5: تأمين HTML والروابط المنشورة

**الأولوية: متوسطة — الحالة: [~] اختبارات الروابط والصور نجحت؛ فحص حقول HTML الأخرى متبقٍ**

- [x] قصر روابط التواصل على HTTPS وصور البيانات على JPEG/PNG/WebP، والتحقق من Base64 الخام؛ اختبار القالب أكد عدم توليد href/src نشطة من مدخلات عدائية.
- [~] مراجعة مواضع الروابط والصور الخارجية في قالب الطبيب؛ اختبار بقية مخرجات HTML وJSON-LD متبقٍ.
- [~] إضافة `rel="noopener noreferrer"` إلى الروابط الخارجية المفتوحة في تبويب جديد؛ اختبار المتصفح متبقٍ.
- [~] اختبار الروابط ومصادر الصور العدائية يغطيه `tests/security-rules.test.mjs`؛ حقول الاسم والوصف وJSON-LD ومراجعة بقية التوليد ما زالت متبقية.

**معيار القبول:** لا ينفذ أي إدخال اختباري شيفرة في المتصفح، وتظل القيم الصحيحة وروابط الصفحات المنشورة تعمل.

## المرحلة 6: اختبارات الانحدار والإطلاق

**الحالة: [~] TypeScript/build و17 اختبار قواعد/API/Worker/برومو ناجحة؛ التكامل والنشر متبقيان**

- [~] أضيفت 17 حالة: 7 قواعد/HTML، و4 Express، و4 Worker/SQLite، و2 promo SQL؛ تكامل ImageKit وCloudflare D1 وXSS لبقية الحقول متبقٍ.
- [~] نجح `npm ci` و`npm run lint` و`npm run build` و`npm run test:security` (17/17، خروج 0)؛ اختبار Cloudflare D1 وImageKit على staging ما زال مطلوبًا.
- [x] فحص اعتماديات `npm ci` بعد override: صفر ثغرات معلنة؛ `pptxgenjs@4.0.1` بقي دون تغيير و`image-size@2.0.4` مثبت.
- [ ] مراجعة فروق القواعد وإعدادات النشر والأسرار قبل أي نشر؛ عدم نشر أسرار أو ملفات بيانات محلية.
- [ ] نشر التغييرات على بيئة اختبار أولًا، ثم إجراء smoke tests لمسارات التسجيل ولوحة الإدارة والنشر والرفع.
- [ ] التحقق من سجلات الأخطاء والرفض بعد الإطلاق، وخطة الرجوع عن تغييرات القواعد أو الـWorker.

**معيار الإنجاز الكلي:** جميع البنود الحرجة والعالية منجزة ومختبرة؛ لا توجد نتائج فشل مفتوحة؛ نجاح البناء والاختبارات موثق؛ اختبارات البيئة المنشورة ناجحة من دون كشف بيانات أو أسرار.

## مراجعة المحاور المطلوبة: المزامنة، موقع الطبيب، ومعلومات chatbot

تاريخ المراجعة: 2026-09-26. هذه النتائج فحص ساكن لمسارات القراءة والكتابة والقوالب؛ لم يحدث نشر أو اختبار على بيانات إنتاجية.

### نتائج التحقق الحالية

- **لوحة الإدارة ليست متزامنة فعليًا مع D1:** قائمة الأطباء تُحمّل من D1 عبر `/api/admin/doctors`، لكن اعتماد/رفض/إيقاف الحساب وتعديل حد الحالات ونسخ النشر تُكتب إلى Firestore. الإعدادات العامة والمقالات تقرأ/تكتب Firestore، والرموز الترويجية D1. لذلك نجاح إجراء إداري في Firestore لا يعني تحديث سجل المستخدم في D1 أو العامل الذي يفرض الصلاحيات.
- **لوحة المستخدم تستخدم D1 للملف والحالات، لكن لا توجد مزامنة لحظية:** تحميل الملف والحالات وحفظهما يتم عبر Worker. الملف يُحمّل عند الدخول ويُحدّث محليًا بعد الحفظ؛ لا يوجد اشتراك أو تحديث تلقائي بين الجلسات. تعديل/حذف/ترتيب الحالة يُحفظ مباشرة منفصلًا عن حفظ الملف.
- **مراجعة تغييرات الحالات المنشورة ناقصة:** حفظ الملف بعد النشر يضع `hasUnreviewedChanges`، لكن مسارات إنشاء/تعديل/حذف/ترتيب الحالات في الـWorker لا ترفع العلامة ولا تسجل إصدار مراجعة. لوحة الإدارة ترشح طابور التعديلات على هذه العلامة، لذلك قد لا تظهر تغييرات الحالات في الطابور.
- **مسار إعادة النشر مقسوم بين مخازن:** موقع الطبيب العام يقرأ لقطة `published_portfolios` من D1 أولًا؛ عند نجاح قراءة لقطة قديمة لا ينتقل إلى Firestore. زر اعتماد الإدارة يكتب بيانات النشر وقراراته إلى Firestore ويولد HTML من كائن D1 محليًا، ثم قد يرسل GitHub Actions. مولد GitHub Actions يقرأ الأطباء المعتمدين من Firestore، بينما التسجيل والحفظ الحاليان يستخدمان D1. ومسار Worker `/api/publish` لا يملك مستدعيًا من الواجهة وهو مقيد للمسؤول. لذلك تعديلات D1 بعد النشر قد لا تحدّث لقطة D1 المنشورة أو الصفحة الثابتة نفسها.
- **القالب المرجعي:** المقارنة البنيوية وجدت أقسام الصفحة الأساسية نفسها (`home`, `skills`, `education`, `cases`, `contact`) وفئات CSS متقاربة؛ CSS المرجع يحوي قواعد `.blog-*` لا يقابلها قسم مرئي في المرجع أو المولد. إذن التصميم الأساسي منقول بدرجة كبيرة، لكن لم يجر اختبار لقطات متصفح يثبت التطابق الكامل.
- **ربط الصور والحالات غير مكتمل:** نوع الحالة في `src/types.ts` يدعم `photos[]` و`beforePhoto` و`afterPhoto`، وصفحة React المنشورة تقرأ هذه الأشكال؛ مولد HTML الثابت يقرأ أساسًا `photo` أو مفاتيح قديمة ولا يحول `photos[]` أو كائنات الصور الحديثة إلى مصدر صالح. كذلك يحمل القالب عنصر شهادات مخفيًا دائمًا ولا يربطه بقائمة `certifications`، ولا يوجد قسم مقالات داخل صفحة الطبيب.
- **معلومات chatbot المؤكدة الخطأ/المضللة:** يدّعي سلايدر تلقائي كل 3 ثوانٍ رغم عدم وجود مؤقت/منزلق في صفحة الطبيب؛ يصف مقالات المدونة كقسم داخل الموقع رغم أنها غير موجودة في قالب صفحة الطبيب؛ ويعد بظهور Google خلال مدة محددة وبفهرسة أنظمة AI عبر IndexNow، مع أن الكود يرسل إلى واجهة IndexNow وBing ولا يملك تكاملًا مباشرًا مع Google/نماذج AI، ولا يمكن ضمان زمن الفهرسة. كما يصف الترقية غير المحدودة بأنها مجانية 100% دون مصدر سياسة/سعر معتمد، بينما الواجهة تحول طلب الترقية إلى WhatsApp ولا تثبت تكلفتها.
- **معلومات تحتاج صياغة مشروطة لا حذفًا آليًا:** الحفظ التلقائي في معالج البداية يكتب إلى `sessionStorage` في المتصفح؛ أما تعديلات Dashboard على الملف فتحتاج زر حفظ، وتغييرات الحالات تُحفظ عبر طلب منفصل. لذلك ينبغي توضيح الفرق بين مسودة محلية وحفظ الحساب. كذلك تحكم إعدادات إعلان تنزيل CV في ظهور `CvAdGate`؛ لا يصح وعد تنزيل غير مشروط إذا كان الإعلان مفعّلًا.
- **الرابط والنشر:** بعض الأجزاء تستخدم `portfoliohubs.github.io` وأخرى `portfoliohubs.pages.dev`، كما أن رابط الطبيب يعتمد على slug الفعلي لا الاسم بالضرورة. يلزم تثبيت النطاق الأساسي وصيغة الرابط المنشور قبل تعديل نص chatbot.

## المرحلة 7: مصدر موحد لمزامنة لوحة الإدارة

**الأولوية: حرجة — الحالة: [~] مصدر D1 مختار؛ API الاعتماد وإجراءات الإدارة قيد النقل والاختبار**

- [~] اعتماد D1 مصدرًا أساسيًا لملفات الأطباء والحالات والموافقة ولقطات النشر؛ ترحيل إجراءات الإدارة والقراءات القديمة ما زال متبقيًا.
- [ ] نقل إجراءات الإدارة (اعتماد، رفض، تعطيل، تعديل الحد، تحديث بيانات الطبيب) إلى Admin Worker API موثّق، مع مصادقة المسؤول وسجل تدقيق وإرجاع السجل المحفوظ من المصدر المعتمد.
- [~] أضيف `PATCH /api/admin/doctors/{uid}` وقُيد بقائمة حقول؛ اختبار Worker/D1 المحلي أكد الرفض لغير المسؤول وظهور الحالة والحد المعدلين في قائمة D1. نقل إجراءات الإدارة إلى هذا المسار ما زال جاريًا.
- [~] نقلت إجراءات التفعيل/الإيقاف والرفض وتعديل بيانات الطبيب وحد الحالات في AdminDashboard إلى D1؛ اختبار الاعتماد endpoint الجديد واستبدال كتابات Firestore متبقيان.
- [~] أضيف `POST /api/admin/doctors/{uid}/approve` وعميل `approveAdminDoctor`؛ Worker/SQLite أكد رفض slug المحجوز وتحديث الملف واللقطة وقراءة البيانات المسطحة عبر `/api/website/{slug}`. ربط AdminDashboard واختبار Cloudflare D1 المنشور متبقيان.
- [ ] نقل الإعدادات التي يقرأها Worker مثل حد الحالات من Firestore إلى واجهة الإعدادات ذات المصدر نفسه، أو إضافة ترحيل أحادي الاتجاه موثق قبل إزالة القراءة القديمة.
- [ ] إضافة منع تعارض/إصدارات عند تعديل طبيب بالتزامن، ثم تحديث لوحة الإدارة من نتيجة الخادم وإظهار وقت آخر مزامنة وأخطاء الحفظ بدل إعلان نجاح قبل تأكيد المصدر.
- [ ] توحيد نشر المدونات والإعدادات أو إبقاؤها نطاقًا منفصلًا موثقًا؛ لا تعرض لوحة واحدة بيانات من D1 وFirestore على أنها سجل متزامن واحد.

**معيار القبول:** تغيير حالة/حد طبيب من الإدارة ينعكس في D1، Worker، لوحة المستخدم، واللقطة المنشورة؛ الرفض أو فشل أي كتابة يظهر كفشل ولا يترك نسخًا متعارضة؛ اختبارات API تثبت ذلك.

## المرحلة 8: مزامنة لوحة المستخدم ومراجعة تغييرات الحالات

**الأولوية: عالية — الحالة: [ ] لم يبدأ**

- [ ] إظهار مصدر البيانات ووقت آخر جلب، وإضافة إعادة تحميل موثوقة عند العودة للوحة أو بعد الحفظ؛ إن لم تُنفذ مزامنة لحظية فلا يوصف النظام بأنه realtime.
- [ ] اجعل أي تعديل حالة بعد النشر يحدّث `hasUnreviewedChanges`/revision على سجل D1 في العملية نفسها التي تحفظ الحالة، بما يشمل الإنشاء والتعديل والحذف وإعادة الترتيب.
- [ ] أعد الحالة المحلية إلى آخر نسخة مؤكدة أو اعرض إعادة محاولة واضحة إذا فشل حفظ الملف أو الحالة؛ لا تترك تغييرات متفائلة غير محفوظة في الواجهة.
- [ ] أظهر للمستخدم حالة التعديل: مسودة محلية، محفوظة على الحساب، بانتظار المراجعة، منشورة؛ اربط الحالات برسائل حقيقية من API.

**معيار القبول:** تعديل ملف أو صورة حالة من جلسة المستخدم يظهر مرة واحدة في لوحة الإدارة بعد التحديث؛ يظهر في طابور المراجعة عند الحاجة؛ لا تختفي/تدّعي الواجهة الحفظ عند فشل API.

## المرحلة 9: مطابقة قالب الطبيب وربط البيانات وإعادة النشر

**الأولوية: عالية — الحالة: [ ] لم يبدأ**

- [ ] اعتماد `C:\Users\EL-Oroby\OneDrive\Desktop\micky\index.html` كمرجع بصري ثابت، مع اختبار لقطة ومقارنة DOM للأقسام الأساسية على بيانات طبيب اختبار؛ إبقاء قواعد `.blog-*` فقط إذا أضيف قسم مقالات فعلي.
- [ ] إنشاء محول بيانات واحد من نموذج D1 إلى نموذج القالب، يدعم `photos[]` و`beforePhoto` و`afterPhoto` و`photo` القديم، ويحوّل `certifications` إذا تقرر عرضها؛ يرفض البيانات الناقصة بدل إخراج صفحة افتراضية توحي بنجاح النشر.
- [ ] جعل مولد GitHub Actions وExpress يقرآن من مصدر البيانات المعتمد نفسه؛ لا يبقى مولد الإنتاج مربوطًا بقراءة Firestore-only لملفات سُجلت في D1.
- [ ] توصيل اعتماد الإدارة بعملية publish واحدة: تحديث حالة D1، وبناء لقطة `published_portfolios`، وتوليد `public/dr/<slug>/index.html` والمقالات وsitemap، ثم نشر GitHub Pages. تحديثات الطبيب المنشور تنشئ مراجعة جديدة، ولا تستبدل العام قبل الاعتماد.
- [ ] لا تعرض «مفهرس» أو «تم النشر» بناءً على قبول dispatch فقط؛ اعرض حالة job وتأكد من اكتمال البناء والنشر، وعالج فشل أحدهما مع إمكانية إعادة المحاولة.
- [ ] اختبر رابطًا جديدًا وتحديثًا بعد النشر: البيانات والصور والعنوان والـcanonical وsitemap يجب أن تتغير من الإصدار نفسه بلا لقطة D1 قديمة أو HTML مولد من بيانات Firestore ناقصة.

**معيار القبول:** صفحة HTML المولدة تطابق المرجع في الهيكل والأنماط المرئية، وتعرض بيانات ملف/حالات الاختبار الصحيحة؛ تعديل معتمد بعد النشر يظهر على الرابط الثابت وواجهة D1، وغير المعتمد لا يغيّر النسخة العامة.

## المرحلة 10: تصحيح الحقائق في chatbot

**الأولوية: متوسطة — الحالة: [ ] لم يبدأ**

- [ ] استبدال وعد السلايدر التلقائي كل 3 ثوانٍ بوصف العرض الفعلي؛ حاليًا صفحة الطبيب تعرض صورة مفردة لكل بطاقة، رغم دعم نموذج البيانات لعدة صور وعدم تحويلها كلها في المولد.
- [ ] إزالة ادعاء أن شهادات ومقالات قابلة للتحرير تظهر في صفحة الطبيب؛ إما تنفيذ ربط شهادات فعلي وإضافة قسم مقالات، أو شرح أن الشهادات مخفية/غير مدعومة وأن المقالات صفحات منفصلة.
- [ ] إزالة ضمان «الصدارة» ومواعيد Google (3–7 أيام) وAI indexing؛ صياغة محايدة تذكر sitemap وIndexNow لمحركاته المدعومة فقط، وأن Google/أنظمة AI تقرر الفهرسة دون ضمان زمن أو ترتيب.
- [ ] ربط جواب السعر بسياسة منشورة ومعتمدة؛ حتى وجود سياسة، لا تقل «غير محدود مجانًا» أو «مجاني 100% للأبد». وضّح حد الباقة الأساسي (3) وأن الترقية تُطلب عبر WhatsApp ويحددها المسؤول.
- [ ] تصحيح جواب «كل كلمة محفوظة تلقائيًا»: مسودة PortfolioWizard محلية في `sessionStorage`، بينما حفظ الملف في Dashboard يدوي؛ وضّح متى تحفظ البيانات على الحساب وأن تغيير الحالة يُحفظ منفصلًا.
- [ ] اربط رابط الطبيب بالنطاق والـslug الرسميين من الإعدادات، وأزل روابط hard-coded بعد توحيد النطاق في المرحلة 9.
- [ ] راجع كل `id`/استجابة في `src/data/chatbotTree.ts` مع مالك حقيقة (CONFIG، API، UI أو سياسة)، وأضف اختبارًا/مراجعة تمنع رجوع الوعود غير المدعومة.

**معيار القبول:** كل معلومة في الشجرة لها مصدر حالي قابل للفحص؛ الادعاءات الزمنية/المالية غير المضمونة محذوفة أو مصاغة بتحفظ؛ الإجابات تطابق ما يستطيع المستخدم تنفيذه الآن في الواجهة.

## سجل التغييرات

| التاريخ | المرحلة/البند | الملفات المعدلة | التغيير | التحقق والنتيجة | الحالة التالية |
|---|---|---|---|---|---|
| 2026-09-26 | إنشاء الخطة وخط الأساس | `PORTFOLIOHUBS-SECURITY-REPAIR-PLAN.md` | توثيق مراحل الإصلاح بناءً على الفحص الساكن؛ لم تُعدّل ملفات التطبيق | تأكدت الملاحظات الأساسية بنصوص الملفات؛ أدوات Node/npm/Git/Firebase CLI غير متاحة في PATH | جميع مراحل الإصلاح لم تبدأ |
| 2026-09-26 | المرحلة 1: تفويض المسؤول؛ المرحلة 4: صياغة Storage | `firestore.rules`, `storage.rules`, `worker/index.ts`, `src/pages/Login.tsx`, `src/pages/AdminDashboard.tsx` | أزيلت صلاحية المسؤول المبنية على لاحقة البريد؛ أصبح التفويض الإداري يتطلب بريدًا موثقًا مع عنوان صريح أو سجل مسؤول؛ أزيل التسجيل التلقائي لوثيقة المسؤول، وقُيدت قراءتها؛ صُححت استدعاءات Firestore في Storage | لا نتائج للبحث عن شروط لاحقة النطاق؛ تشخيصات المحرر لا تعرض أخطاء في ملفات TypeScript؛ تعذر تشغيل البناء واختبارات Firebase لعدم توفر Node/npm/Firebase CLI | التنفيذ جارٍ؛ يلزم اختبار قواعد وتدفق المسؤول قبل الإغلاق |
| 2026-09-26 | المرحلة 2: حماية Express | `server.ts`, `src/pages/AdminDashboard.tsx` | أضيف تحقق Firebase JWT للبريد الموثق وقائمة المسؤولين الصريحة إلى واجهتي التوليد وIndexNow؛ قُيدت روابط IndexNow لأصل الموقع؛ ترسل لوحة الإدارة ID token | البحث أكد وجود المصادقة والتحقق؛ تشخيصات المحرر لا تعرض أخطاء؛ اختبار API والبناء متعذران لغياب Node/npm | المصادقة مضافة؛ يلزم تشغيل اختبارات 401/403/200 والتحقق من حدود المدخلات |
| 2026-09-26 | المرحلة 3: حدود الباقات والبرومو | `worker/index.ts`, `src/pages/PortfolioWizard.tsx` | حفظ حدود/حالة الحساب كحقول خادمية، فرض حصة الحالات عند الإنشاء، ونقل تحقق البرومو ومنح الحد إلى Worker/D1؛ إزالة حفظ الحد من المتصفح | تشخيصات المحرر لا تعرض أخطاء؛ فحص SQL بـSQLite تعذر لأن Python غير مثبت (اختصار المتجر فقط)؛ اختبارات D1 والتزامن لم تُشغّل | يلزم تثبيت/إتاحة بيئة تشغيل، وفحص الاستعلامات في محاكي/بيئة D1 واختبار الطلبات المتزامنة |
| 2026-09-26 | المرحلة 4: رفع الوسائط | `storage.rules`, `worker/index.ts` | تقييد ملفات Storage/Worker إلى 5MB وصور JPEG/PNG/WebP؛ إجبار ImageKit على مجلد UID؛ التحقق من ملكية الملف قبل تسجيله في D1 | بحث ساكن أكد القيود والتحقق؛ تشخيصات Worker بلا أخطاء؛ اختبار Storage Emulator وImageKit/D1 متعذر لغياب الأدوات | يلزم تحقق المحاكي واختبار حسابين وملفات صحيحة/مرفوضة |
| 2026-09-26 | المرحلة 5: HTML المنشور | `scripts/doctor-template.mjs` | تصفية روابط التواصل إلى HTTPS وصور البيانات إلى أنواع محددة؛ منع `//` في مسارات الصور النسبية؛ إضافة `noopener noreferrer` للروابط الخارجية | لا نتائج للروابط الخام؛ تشخيصات المحرر بلا أخطاء؛ اختبار توليد HTML والمتصفح متعذر لغياب Node | يلزم تشغيل اختبارات مدخلات XSS ومعاينة الصفحة المولدة |
| 2026-09-26 | المرحلة 2: التحقق وحدود طلبات Express | `server.ts` | خفض حدود JSON/urlencoded؛ التحقق من شكل الطبيب وعدد/نوع الحالات؛ إخفاء تفاصيل الأخطاء وإضافة رؤوس حماية أساسية | البحث أكد القيود؛ تشخيصات المحرر بلا أخطاء؛ اختبارات API والبناء غير متاحة لغياب Node/npm | اختبار المدخلات والـAPI وتحديد المعدل ما زال مطلوبًا |
| 2026-09-26 | المرحلة 3: توحيد إدارة البرومو | `src/pages/AdminDashboard.tsx`, `firestore.rules` | نقل عرض/إنشاء/تعديل/حذف الرموز إلى Worker/D1؛ استيراد الرموز القديمة غير الموجودة في D1؛ إزالة تحديث العداد المسموح للعميل في Firestore | تشخيصات لوحة الإدارة بلا أخطاء؛ لا يمكن تنفيذ اختبار D1/Firestore دون Node/Firebase CLI | يلزم اختبار الترحيل والاسترداد وحد الاستخدام بالمحاكي |
| 2026-09-26 | المرحلة 3: سباق استرداد البرومو | `worker/index.ts` | ربط تعديل ملف المستخدم بنتيجة `changes() = 1` للزيادة السابقة داخل دفعة D1، لمنع منح الحد عند فشل زيادة العداد بسبب التزامن | تشخيص Worker بلا أخطاء؛ اختبار التزامن الفعلي غير ممكن دون D1/محاكي | تحقق بالتزامن وقرر هل يسمح بإعادة استخدام الرمز من الحساب نفسه |
| 2026-09-26 | إصلاحات الأنواع والتحقق | `server.ts`, `scripts/doctor-template.mjs`, `src/lib/cloudflareApiClient.ts` | تعريف نوع الحالات المقبولة وحدّ تمرير القالب، وتحديث نوع استجابة استرداد البرومو | `npm run lint` نجح؛ لم يُشغّل البناء بعد | تشغيل البناء ومراجعة تقرير اعتماديات npm |
| 2026-09-26 | المرحلة 0 و6: التثبيت والتحقق | `package.json`, `package-lock.json` | إضافة override لـ`image-size@^2.0.4` تحت `pptxgenjs` من دون تخفيض `pptxgenjs@4.0.1` | `npm ci --legacy-peer-deps` نجح؛ أظهر صفر ثغرات؛ `npm run lint` و`npm run build` نجحا؛ Wrangler D1 محلي أكد 3 حالات ورفض الرابعة؛ SQLite أكد عدم منح الحد في محاولة برومو قديمة. بقي تحذير Vite بشأن `__dirname` وchunk بحجم 2.2MB تقريبًا | Firebase Emulator وAPI/ImageKit الفعلي واختبار Worker/D1 ما زالت متبقية |
| 2026-09-26 | المرحلة 1 و4 و5 و6: اختبارات القواعد والقالب | `package.json`, `package-lock.json`, `tests/security-rules.test.mjs`, `scripts/doctor-template.mjs` | إضافة `@firebase/rules-unit-testing` وأمر `test:rules`؛ إضافة 7 اختبارات للتفويض، خصوصية وثائق المسؤول، منع تعديل عداد البرومو، ملكية/نوع/حجم Storage، الروابط ومصادر الصور؛ رفض Base64 خام غير صالح في مولد الصفحات | `firebase emulators:exec --only firestore,storage ... npm run test:rules`: 7/7 ناجحة؛ `npm ci` أظهر صفر ثغرات؛ `npm run lint` و`npm run build` ناجحان | Worker/API/ImageKit وتدفق المسؤول والاختبارات المتبقية في المراحل 1-6 ما زالت بحاجة إلى تكامل فعلي |
| 2026-09-26 | المرحلة 3: موافقة النشر | `worker/index.ts` | قصر `POST/PUT /api/publish` على المسؤول مؤقتًا لأن المسار ينشر إلى D1 ولا يتحقق من موافقة الحساب، ولا يملك مستدعيًا داخل الواجهة الحالية | تشخيص Worker بلا أخطاء؛ تحقق ساكن أكد شرط المسؤول؛ اختبار API غير متاح حتى الآن | اختبار رفض المستخدم العادي، وربط النشر لاحقًا بمصدر موافقة D1 عند تفعيل المسار للمستخدم |
| 2026-09-26 | المرحلة 6: توحيد تشغيل الاختبارات | `package.json` | إضافة `test:rules:emulated` لتشغيل Firebase CLI 15.31 على demo project ثم الاختبارات | `npm run test:rules:emulated`: 7/7 ناجحة، exit 0 | احتفظ بمشروع demo؛ لا تستخدم هذا الأمر مع إعدادات production |
| 2026-09-26 | المرحلة 1: توحيد مصدر المسؤولين | `firestore.rules`, `storage.rules`, `worker/index.ts`, `server.ts`, `wrangler.toml`, `src/pages/Login.tsx`, `src/pages/AdminDashboard.tsx`, `tests/security-rules.test.mjs` | أُزيلت صلاحية المسؤول من وثائق Firestore وD1 ومتغير `ADMIN_EMAILS`؛ كل الطبقات تقبل الآن الحسابات الثلاثة الموثقة نفسها؛ أضيف اختبار لوثيقة مسؤول ديناميكية لا ترفع الصلاحية | البحث أكد زوال الشروط الديناميكية؛ `test:rules:emulated` نجح 7/7؛ `npm run lint` نجح | آلية إضافة مسؤوليْن جدد متوقفة حتى اعتماد custom claims/Provisioning موحد واختبار واجهات Worker/Express |
| 2026-09-26 | تحقق نهائي بعد توحيد التفويض | الملفات المعدلة أعلاه | إعادة بناء المشروع بعد توحيد المصدر وإزالة الاستيرادات غير المستخدمة | `npm run lint` ناجح؛ `npm run build` ناجح ويولّد صفحة الدكتور؛ المحاكي 7/7 و`npm audit` صفر ثغرات من التثبيت النظيف | متبقي اختبارات Worker/Express/ImageKit، تحديد معدل Express، وآلية مستقبلية لإضافة مسؤولين جدد |
| 2026-09-26 | المرحلة 2 و6: اختبارات API وتحديد المعدل | `server.ts`, `package.json`, `tests/server-api.test.ts` | فصل إنشاء Express عن تشغيله للاختبار؛ إضافة حد 20 طلبًا/دقيقة لكل UID ومسار؛ اختبار JWT محلي عبر JWKS تجريبي وحالات 401/403/400/429 | `npm run test:api`: 4/4 ناجحة؛ `npm run lint` نجح قبل الاختبار؛ تحديد المعدل داخل ذاكرة العملية | للتشغيل متعدد النسخ استخدم rate limit مشتركًا أو proxy؛ اختبار عمليات كتابة متزامنة وتكامل Express الكامل متبقي |
| 2026-09-26 | المرحلة 3 و6: منع تكرار البرومو واختبارات Worker | `migrations/0004_promo_redemptions.sql`, `worker/index.ts`, `tests/promo-redemption.test.mjs`, `tests/worker-api.test.ts`, `package.json` | حفظ استرداد فريد لكل `(code, uid)` وربط زيادة العداد ومنح الحد ذريًا؛ اختبار Worker الفعلي فوق D1-compatible SQLite adapter ومخططات migration | migration نفذت على D1 محلي؛ promo SQL 2/2، Worker 4/4، rules 7/7، Express 4/4؛ `npm run test:security` نجح 17/17 بخروج 0؛ lint/build ناجحان | اختبار Cloudflare D1 المنشور وmigration على staging وImageKit الحقيقي ومشاركة rate limit بين نسخ الخادم متبقية |
| 2026-09-26 | المرحلة 6: مجموعة الأمان المجمعة | `package.json` | جمع اختبارات القواعد والقالب وExpress في `npm run test:security` تحت محاكيات demo | 11/11 اختبارًا ناجحًا، exit 0؛ `npm run lint` و`npm run build` ناجحان بعدها | اختبارات Worker/D1/ImageKit والرفع الفعلي والاختبار على staging متبقية |
| 2026-09-26 | المرحلة 3: منع استرداد البرومو المتكرر | `migrations/0004_promo_redemptions.sql`, `worker/index.ts` | إضافة جدول بمفتاح أساسي `(code, uid)`؛ ربط التسجيل والعداد والترقية في دفعة مشروطة، وحذف سجلات الاسترداد عند حذف الرمز | migration تنفذ على D1 المحلي؛ فحص TypeScript ناجح؛ اختبار Worker/D1 للاسترداد الأول والثاني والتزامن متبقٍ | تطبيق migration في بيئة غير إنتاجية وتشغيل اختبارات لكل مستخدم وحد متزامن قبل الترحيل/النشر |
| 2026-09-26 | تحقق نهائي بعد اختبارات D1 المحلية | `worker/index.ts`, `migrations/0004_promo_redemptions.sql`, `tests/promo-redemption.test.mjs`, `tests/worker-api.test.ts`, `package.json` | إضافة حماية إعادة استخدام البرومو واختبارات Worker/SQL إلى المجموعة المجمعة | `npm run test:security`: 17/17، exit 0؛ `npm run lint` و`npm run build` ناجحان؛ بقي تحذير Vite عن `__dirname` وchunk كبير | متبقي Cloudflare D1 الفعلي، ImageKit، rate limit مشترك، واختبارات staging؛ لم يحدث نشر إنتاجي |
| 2026-09-26 | المرحلة 7: اعتماد طبيب ونشر لقطة D1 | `worker/index.ts`, `tests/worker-api.test.ts` | إضافة `POST /api/admin/doctors/{uid}/approve` لربط slug وتحديث حالة المستخدم والبورتفوليو واللقطة المنشورة ضمن D1 batch، وتسطيح بيانات الحالات | اختبار Worker مع migration محلية أكد slug collision 409، ونشر slug جديد، وقراءة `/api/website/{slug}` لبيانات الطبيب والحالة الصحيحة | ربط زر اعتماد الإدارة بالـendpoint وإيقاف كتابات Firestore المتوازية ثم إعادة اختبار التدفق الكامل |
| 2026-09-26 | مراجعة لوحتي التحكم والقالب وchatbot | `PORTFOLIOHUBS-SECURITY-REPAIR-PLAN.md` | توثيق اختلاف مصدر الإدارة بين D1 وFirestore، علامات مراجعة الحالات والنشر، فجوات photos/certifications في المولد، والحقائق غير المدعومة في chatbot؛ إضافة المراحل 7–10 ومعايير قبولها | مراجعة ثابتة لمسارات القراءة/الكتابة؛ مقارنة أقسام/CSS المرجع مع المولد؛ تحقق نصوص chatbot من التنفيذ؛ لا اختبارات جديدة ولا تغييرات تطبيق في هذه الخطوة | تنفيذ المراحل 7–10 بالترتيب؛ تبدأ باعتماد مصدر بيانات وحيد، ثم اختبارات المزامنة/النشر، بيانات القالب، وتصحيح رسائل chatbot |
